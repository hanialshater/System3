# What doesn't work

Dead ends from actually running the [try-search-eval-improve loop](../process/try-search-eval-improve-loop.md). Keep every entry, even after the underlying file is fixed — the point is that the next agent doesn't waste a cycle re-discovering the same failure. Newest entries at the top.

Format:

```
## YYYY-MM-DD — short title

**Tried:** what was generated, against what kind of session/signals.
**Failed because:** the specific reason (which rubric item it failed, or what outcome contradicted it).
**Retired/fixed:** which file was changed as a result, and how — link it.
```

---

*(No entries yet — see [what-seems-to-work.md](what-seems-to-work.md) for the same note. Both files fill in together as the loop actually runs.)*
